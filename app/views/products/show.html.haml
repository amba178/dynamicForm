%h2{class: "text-center"} product details
%hr 
%h3{class: "text-center"}="#{@product.name } #{number_to_currency @product.price}"
%table{class: "table table-hover"}
	%thead
		%tr
			%th Feature Name
			%th Feature Value 
			
	%tbody
		-@product.properties.each do |name, value|
			%tr
				%td=name.humanize
				%td=value.is_a?(Array) ? value.reject!{|e| e.empty?}.join(", ") : value 

= link_to 'Edit', edit_product_path(@product), class: "btn btn-primary"			
		
=link_to products_url, class: "btn btn-primary" do 
	%span{class: "glyphicon glyphicon-backward"} 
	product list
